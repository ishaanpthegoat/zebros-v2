import type { Value } from "./types";

// The four team values (PRD §4.3). Titles and copy live on the current site and
// are NOT in the PRD — pending content (Phase 1 content gap). The About page
// renders a clean fallback while this is empty.
// TODO: load the four values (icon, title, body) once sourced.
export const values: Value[] = [];
